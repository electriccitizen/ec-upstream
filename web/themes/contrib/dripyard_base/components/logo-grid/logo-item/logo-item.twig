<div class="logo-item">
  {% if href %}
    <a href="{{ href }}" class="logo-item__link">
  {% endif %}

    {{ include('dripyard_base:image-or-media', {
      image,
      width: 400,
    }, with_context = false) }}

  {% if href %}
    </a>
  {% endif %}
</div>
