component:
  id: citizen_sdc:views-news-list
  label: 'Views News List'
  description: 'Thin child of `citizen_sdc:views-unformatted-base` for the `content_list.news_list` view display. Exists so display-specific SCSS auto-attaches on these renders.'
  status: experimental

props:
  type: object
  required:
    - rows
  properties:
    attr:
      type: object
    title:
      type: [string, 'null']
    default_row_class:
      type: boolean
    rows:
      type: array
