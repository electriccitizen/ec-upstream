{# Pass-through to views-unformatted-base. Nothing is added to the
   markup here — the point of this SDC is to give per-display SCSS a
   home + auto-attach a library on renders of bios.all_bios. #}
{{ include('citizen_sdc:views-unformatted-base', {
  attr: attr,
  title: title,
  default_row_class: default_row_class,
  rows: rows,
}, with_context = false) }}
