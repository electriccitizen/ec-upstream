{# Pass-through to views-unformatted-base. #}
{{ include('citizen_sdc:views-unformatted-base', {
  attr: attr,
  title: title,
  default_row_class: default_row_class,
  rows: rows,
}, with_context = false) }}
