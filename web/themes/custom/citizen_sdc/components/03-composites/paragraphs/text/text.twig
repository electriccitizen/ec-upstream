{% set classes = [
  'text',
  'paragraph',
  'paragraph--type--text'
] %}

<div class="{{ classes|join(' ') }}">
  {% if title %}
    <h2 class="widget-title">{{ title }}</h2>
  {% endif %}

  <div class="widget-content">
    {{ slots.content }}
  </div>
</div>
