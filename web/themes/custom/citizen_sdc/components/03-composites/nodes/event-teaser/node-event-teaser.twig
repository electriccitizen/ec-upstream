{% set teaser_content %}
  {% if dates %}
    {{ dates }}
  {% endif %}
  {% if summary %}
    {{ summary }}
  {% endif %}
{% endset %}

{{ include('citizen_sdc:node-teaser-base', {
  attr: attr,
  modifier_class: 'event',
  title: title,
  url: url,
  title_attr: title_attr,
  image: image,
  content: teaser_content,
}, with_context = false) }}
