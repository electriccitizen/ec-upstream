{#
/**
 * @file
 * Basic page default/full view — delegates to node-basic-page-default SDC.
 * Template file uses the bundle machine name (`page`); the SDC folder/id uses
 * the readable name (`basic-page`).
 */
#}
{% import '@citizen_sdc/macros/node-layout-sections.html.twig' as node_macros %}

{{ include('citizen_sdc:node-basic-page-default', {
  attr: attributes,
  title_prefix: title_prefix,
  title_suffix: title_suffix,
  content: node_macros.wrap_layout_builder_sections(content),
}, with_context = false) }}
