{#
  Drupal paragraph template for the `horizontal_rule` bundle. Pulls the
  color key off field_color and hands it to the SDC. Falls back to the
  field's config default (`light_gray`) when the item is missing — legacy
  paragraphs can pre-date the required flag and leave no storage row,
  which would otherwise trip Html::getClass() in the SDC.
#}
{{ include('citizen_sdc:horizontal-rule', {
  attr: attributes,
  color: paragraph.field_color.0.value|default('light_gray'),
}, with_context = false) }}
