{#
  Display-specific suggestion for `news.homepage_news`. Routes to
  `citizen_sdc:views-homepage-news` so its SCSS auto-attaches.
#}
{{ include('citizen_sdc:views-homepage-news', {
  title: title,
  default_row_class: default_row_class,
  rows: rows,
}, with_context = false) }}
